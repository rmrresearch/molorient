[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "geom_standardizer"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
molorient = "molorient.main:main"